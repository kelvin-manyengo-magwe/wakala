import { StyleSheet } from 'react-native';


export const styles = StyleSheet.create({
  container: {
    padding: 16,
    paddingTop: 10,
    alignItems: 'center',
  },

    xArrow: {
      position: 'absolute',
      bottom: 31,
      right: 1,
      zIndex: 1,
    },
    yAxisLabel : {
        left: -138,
        fontWeight: 'bold',
        fontSize: 12,
        },
});