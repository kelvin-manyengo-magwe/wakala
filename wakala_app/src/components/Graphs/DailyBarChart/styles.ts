import { StyleSheet } from 'react-native';


export const styles = StyleSheet.create({
  container: {
    padding: 16,
    paddingTop: 10,
    alignItems: 'center',
  },
   yArrow: {
      position: 'absolute',
      top: 0,
      left: 18.5,
      zIndex: 1,
    },
    xArrow: {
      position: 'absolute',
      bottom: 30,
      right: 0,
      zIndex: 1,
    },
});