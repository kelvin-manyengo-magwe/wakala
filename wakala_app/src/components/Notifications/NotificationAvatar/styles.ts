import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
    avatarContainer: {
        justifyContent: 'center',
        alignItems: 'center',
        marginRight: 15,
    },
    avatarLetter: {
        color: '#FFFFFF', // White letter for better contrast on colored backgrounds
        fontWeight: 'bold',
    },
});



export default styles;